# PASS 1 — VISUAL AUDIT

**Status:** F G H 적용됨. I(캐리 정수 스케일)도 같이 넣음. PASS 2 대기.

분석 대상: 웹 세로 슬라이스 (SCHOOL / CLUB / FEST / ARENA).  
Unity Scene / Prefab / Pixel Perfect Camera **없음**. 코드는 수정하지 않음.

이전 PASS 1 A–E(케이블 명도, 구역 1px, 캐리 방향, fillText 제거, `!` 좌표 분리)는 이미 들어가 있다. 이 문서는 **그 이후 현재 화면** 기준이다.

| 실제 구조 | 경로 |
|---|---|
| Camera | `src/camera.js` — 320×180 정수 스케일, 팔로우 없음 |
| World / HUD | `src/game.js` — `drawWorld` / `drawEntities` / `drawFx` / `drawHud` |
| Palette | `src/palette.js` 32색 |
| Sprites | `assets/sprites/` + `tools/gen_sprites.py` |
| Venues | `src/venues.js` |
| Filter | canvas `imageSmoothingEnabled = false`, CSS `pixelated` |

---

## 1. 가장 먼저 눈에 띄는 시각적 문제 5개

1. **FEST 무대 바닥이 `DANGER_RED` / `GUITAR_RED` 체커다.** 사고 색이 바닥이 되면 `!`와 관객이 묻힌다. 학교 축제와 같은 게임이 아니다.
2. **장비(앰프·드럼·스탠드 기타)가 엔티티보다 먼저 그려진다.** 플레이어는 항상 장비 앞에 선다. 가림 계층이 없다.
3. **사고 `!`가 네 종류 모두 같은 8×12 bang 스프라이트다.** 위치는 갈렸지만 “무엇이 고장인지”는 아이콘으로 안 읽힌다.
4. **든 기타/마이크를 16×24 → 10×16으로 늘려 그린다.** 픽셀이 찌그러진다.
5. **CLUB/ARENA가 INTERACTABLE·SUCCESS 색을 조명/러그로 쓴다.** `PEDAL_BLUE` 러그, `TENSION_PINK` 스포트. 팔레트 역할이 깨진다.

---

## 2. Pixel consistency

| 항목 | 결과 |
|---|---|
| 내부 해상도 320×180, 정수 배율 | 통과 |
| Point filter / AA 없음 | 통과 |
| 타일 16, 캐릭터 16×24, 소품 8~32 | 시트는 통과 |
| PPU / Compression / Unity import | 해당 없음 (웹 PNG) |
| draw 시 `Math.round` | 대체로 통과 |
| `fillText` | **제거됨. 통과** |
| `drawCarried` dest 10×16 | **비정수 스케일. 위반** |
| squash 시 dest width +1 | 의도된 1px. 허용 가능 |
| `!` sin bob | 정수 px. 형식은 맞음 |
| FEST/CLUB 8×8, 8×4 fillRect 바닥 | 타일 16 그리드와 밀도 다름 |

서로 다른 PNG 해상도가 섞여 있지는 않다. 깨지는 쪽은 **캐리 스케일과 공연장 fillRect 장식**이다.

---

## 3. 캐릭터 가독성

- 방향: 4방향 시트. 좌우 앞뒤는 읽힘.
- P1 파랑: SCHOOL 나무 위에서는 보임. CLUB `PEDAL_BLUE` 러그 위에서는 셔츠와 바닥이 같은 가족.
- P2 노랑: SCHOOL에서 가장 잘 보임.
- 기타리스트 검정 조끼 + 어두운 앰프: 붙어 있으면 한 덩어리. A–E 이후에도 남음.
- 드러머: 앉은 실루엣이 약함. 키트 32×24가 더 크게 읽힘.
- 든 아이템: 바라보는 쪽으로 옮김 (A–E). 멀리서는 보이지만 10×16 왜곡으로 기타 실루엣이 뭉개짐.
- NPC는 항상 `down`. 연주 방향이 없음.
- 장비가 월드 레이어라 캐릭터가 드럼 뒤로 못 들어간다. “뒤에 이상하게 가려짐”이 아니라 **아예 가려지지 않음**.

---

## 4. Stage / Backstage / Audience 구분

목표: 글자 없이 환경으로 구분.

| 구역 | SCHOOL | CLUB / FEST / ARENA |
|---|---|---|
| AUDIENCE | 어두움 + 레일 + 작은 크라우드. 가장 성공 | FEST 별 1px, ARENA 대각 스포트가 장식처럼 싸움 |
| STAGE | 우드 타일. 영역 보임 | FEST=빨강 사고바닥. CLUB=파란 러그. ARENA=금속 |
| BACKSTAGE | 어두운 타일 + 1px 경계 (A–E) | **네 곳 모두 같은 나무 타일 48.** 클럽/아레나도 나무 창고 |
| INTERACTABLE | 예비 기타, 밝은 케이블/잭 | 러그/스포트가 더 눈에 띔 |
| ACCIDENT | 빨간 `!` | FEST 바닥과 충돌 |

SCHOOL만 ART_GUIDE 레이아웃을 지킨다. 나머지 세 곳은 배경 교체 + 팔레트 역할 위반이다. PASS 18 기준으로는 Stage가 아니다.

---

## 5. 사고 시선 유도

되는 것: 빨간 `!` 채도는 높다. SCHOOL에서는 “어디쯤”은 잡힘. 좌표는 앰프 / 기타리스트 / 드러머 / 보컬로 갈렸다.

안 되는 것:

- 네 사고의 `!` 그림이 같다. 종류는 안 갈림.
- FEST에서는 `!`가 빨간 바닥 위에 올라간다.
- 장비 Sprite 자체는 사고 전후가 거의 같다. “어? 기타가 안 들리는데?”는 여전히 귀에 의존.
- 피드백 1px 가로선은 마이크가 아니라 화면을 가리킨다.
- 튜토리얼 화살표도 DANGER라 `!`와 경쟁.
- 사고/수리 모두 전역 Camera Shake 1px. 시선이 지점이 아니라 화면.

---

## 6. UI 문제

- 인게임 `fillText`는 없음. 타이머·타이틀은 3×5 픽셀 글리프.
- 타이틀/윈/루즈는 `BG_SHADOW` 큰 사각형 패널. 월드 위에 붙는 대시보드 조각.
- Hold 수리는 캐릭터 위 노란 16×2 바. 월드 피드백이 아님.
- 만족도 숫자 바는 없음. 대신 관객 머리 위 1px `TENSION_PINK` 막대. HP로 안 읽힘.
- 캔버스 밖 HTML hint (`A/D 무대 선택…`)는 시스템 12px 폰트. 픽셀 그리드 밖.
- 범위 안 `E` 글리프는 금색. 상호작용 힌트로는 명확. 사고 UI와는 별개.

화면 대부분은 월드다. 문제는 UI 양이 아니라 **공연장 장식이 UI/사고 색을 훔친 것.**

---

## 7. Camera 문제

- 줌/팔로우 없음. 320×180 전체가 항상 보임.
- PASS 4 목표(“전체를 완벽하게 감시할 수는 없는 정도”)와 **반대**.
- 캐릭터 16×24 / 화면 높이 180은 크기감은 괜찮음.
- Shake는 pickup/이동에는 없음. 사고·수리마다 전역 1px.

PASS 1에서 카메라 리디자인은 하지 말 것.

---

## 8. 현재 미감을 가장 크게 망치는 요소 3개

1. **FEST/CLUB/ARENA의 역할 없는 색** — 같은 32색이지만 BACKGROUND에 DANGER/SUCCESS/PEDAL을 씀.
2. **장비 z-order** — 무대가 종이인형 한 장. 깊이 없음.
3. **사고 = 동일한 `!` + 동일 장비 스프라이트** — 소리가 꺼져도 눈이 “고장”을 못 봄.

---

## 9. 효과가 클 수정 5개

| ID | 수정 | 왜 큰가 |
|---|---|---|
| F | FEST/CLUB/ARENA 바닥을 STAGE/BACKGROUND 역할 색만 쓰게 | 화면이 다시 한 게임이 됨 |
| G | 앰프·드럼·스탠드를 엔티티와 y정렬 | 무대가 납작하지 않음 |
| H | 사고 중 장비 스프라이트 변화 (잭 빔, 줄, 스틱 없음) | 귀+눈이 같이 “고장” |
| I | 캐리 dest를 정수 픽셀 (16×24 또는 8×12). 비정수 스트레치 금지 | 픽셀 밀도 회복 |
| J | 관객 1px 장식 제거, mood 프레임만으로 만족도 | PASS 12의 입구. UI 없이 HP |

A–E는 유지. 다시 하지 않음.

---

## 10. 수정 우선순위

```text
READABILITY
  F 공연장 바닥 색 역할을 STAGE/BG로 되돌림
  H 사고 시 장비 형태 변화
  G 장비 y정렬
↓
CONSISTENCY
  I 캐리 정수 스케일
  J 관객 1px 장식 정리
  백스테이지 타일을 공연장마다 바꾸지 말고, 우선 SCHOOL 규칙을 네 곳에 재사용
↓
아직 하지 말 것
  카메라 줌 (PASS 4)
  이동/집기 juice (PASS 5~7)
  새 사고, 새 역할, 새 맵 메카닉
  타이틀 패널 리디자인 (PASS 3)
```

추천 승인 묶음: **F G H**  
SCHOOL만 먼저 해도 된다. CLUB/FEST/ARENA는 콘텐츠 확장 이전에 색 역할만 고치는 것이 미감을 덜 무너뜨린다.

---

다음: 위 묶음을 승인하면 해당 항목만 수정하고 멈춘다. PASS 2 Color System 전체를 한 번에 하지 않는다.
