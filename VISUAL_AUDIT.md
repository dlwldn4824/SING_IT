# PASS 1 — VISUAL AUDIT

분석 대상: 웹 세로 슬라이스. Unity Scene / Prefab / Pixel Perfect Camera **없음**.

| 실제 구조 | 경로 |
|---|---|
| Camera | `src/camera.js` — 320×180 정수 스케일, 팔로우 없음 |
| Draw / UI | `src/game.js` — `fillText`, `!`, 튜토리얼 바 |
| Palette | `src/palette.js` 32색 |
| Sprites | `assets/sprites/` + `tools/gen_sprites.py` |
| Filter | canvas `imageSmoothingEnabled = false`, CSS `pixelated` |

코드는 수정하지 않음.

---

## 1. 가장 먼저 눈에 띄는 시각적 문제 5개

1. **검정 케이블이 어두운 백스테이지에 묻힌다.** 예비 기타(금색)가 더 잘 보여서 “가져갈 것”이 틀린 곳으로 간다.
2. **인게임 글자가 시스템 `8px monospace fillText`다.** 픽셀 그리드에 안 붙는다. 튜토리얼 자막이 디버그 콘솔처럼 보인다.
3. **든 아이템이 머리 위 고정** (`y - 14`). 기타 16×24가 캐릭터를 가린다.
4. **P1 파랑 셔츠와 관객 파랑이 같은 가족.** “내가 누구인지”가 관객 줄과 섞인다.
5. **무대와 백스테이지가 갈색 vs 조금 더 어두운 갈색뿐.** 발밑만 보면 같은 나무 방.

---

## 2. Pixel consistency

| 항목 | 결과 |
|---|---|
| 내부 해상도 320×180, 정수 배율 | 통과 |
| Point filter / AA 없음 | 통과 |
| 타일 16, 캐릭터 16×24, 소품 8~32 | 대체로 통과 |
| PPU / Compression / Unity import | 해당 없음 (웹 PNG) |
| 이동 좌표 float + draw `Math.round` | **0.5 근처 1px 지터 가능** |
| squash 시 dest width +1 | 의도된 1px 왜곡. 허용 가능 |
| `fillText` | **픽셀 밀도가 스프라이트와 다름. 최대 위반** |
| `globalAlpha 0.35` 피드백 파동 | 부드러운 레이어. 픽셀 규칙과 충돌 |
| 카메라 쉐이크는 정수 px | 형식은 맞음. 남용은 Camera 패스 문제 |

서로 다른 해상도 시트가 섞여 있지는 않다. 깨지는 쪽은 **에셋이 아니라 텍스트와 반투명 라인**이다.

---

## 3. 캐릭터 가독성

- 방향: 4방향 시트가 있어 좌우 앞뒤는 읽힘.
- P2 노랑: 무대 갈색 위에서 가장 잘 보임.
- P1 파랑: 무대에선 보이지만 관객과 충돌.
- 기타리스트 검정 조끼 + 어두운 앰프: 장비 뒤에 붙으면 한 덩어리.
- 드러머: 앉은 실루엣이 약함. 키트가 키보드처럼 읽힘.
- 들고 있는 아이템: 머리 위라 손과 분리. 멀리서는 “떠 있는 기타”.
- y정렬 드로우가 있어 앞뒤 가림은 동작함. 캐릭터가 앰프 뒤에 비정상적으로 클립되진 않음.
- P2가 무대에 서 있으면 네 번째 밴드 멤버로 오인 가능 (스태프 vs 밴드 실루엣 차이 부족).

---

## 4. Stage / Backstage / Audience 구분

목표: 글자 없이 환경으로 구분.

| 구역 | 지금 |
|---|---|
| AUDIENCE | 가장 성공. 어두운 바닥 + 레일 + 작은 크라우드 |
| STAGE | 밝은 우드. 영역은 보임 |
| BACKSTAGE | STAGE와 재질이 같음. 명도만 한 단계 |
| INTERACTABLE | 예비 기타만 명확. 케이블/앰프 실패 |
| ACCIDENT | `!` 색은 강함. 환경 변화는 약함 |

조명 차이가 없다. 백스테이지가 “스태프 공간” 구조(선반, 박스 정렬, 바닥 스트립)가 아니라 그냥 아래쪽 어두운 줄이다.

---

## 5. 사고 시선 유도

되는 것: 빨간 `!` 는 화면에서 가장 채도가 높다. “어디쯤”은 잡힘.

안 되는 것:

- 케이블 사고와 줄 사고 `!` 좌표가 둘 다 기타 (156, 86). **무엇이 고장인지 안 갈림.**
- 튜토리얼 화살표도 DANGER_RED라 `!`와 경쟁.
- 피드백은 화면 가로 전체 1px 파동. 시선이 마이크가 아니라 화면이 됨.
- 사고 시 전역 Camera Shake → 시선이 사고 지점이 아니라 화면 전체가 흔들림.
- Stem이 빠지는 건 귀로는 강함. 눈으로는 앰프/기타 Sprite가 거의 안 바뀜. “어? 기타가 안 들리는데?”는 오디오에만 기대고 있다.

---

## 6. UI 문제

- 튜토리얼 하단 바 + 한글 `fillText` = 월드가 아니라 자막.
- Hold 수리 = 캐릭터 위 노란 16×2 바. 범용 HP바.
- 타이머 상단 중앙 텍스트. 관객 HP 바는 없음 (관객 자체가 바여야 하는데 크라우드 변화가 약함).
- 캔버스 밖 HTML hint. 게임과 다른 타이포.
- 사고 텍스트 팝업은 없음 → 이 부분은 방향이 맞음.

화면 대부분은 월드다. 문제는 UI가 많아서가 아니라 **있는 UI가 픽셀 게임에 속하지 않는 것.**

---

## 7. Camera 문제

- 줌/팔로우 없음. 320×180 전체가 항상 보임.
- POLISH_PLAN PASS 4 목표(“전체를 완벽하게 감시할 수는 없는 정도”)와 **반대**. 사고는 찾는 게 아니라 즉시 보인다.
- 캐릭터 16×24 / 화면 180 높이는 크기감은 괜찮음. 너무 크지 않음.
- Shake가 사고 발생마다 전역. Pickup/이동에는 없음 → 남용까지는 아님. 다만 작은 사고에도 화면이 가서 “큰 사고” 연출 예산이 없음.

PASS 1에서 카메라 리디자인은 하지 말 것. 가독성 수정 후 PASS 4.

---

## 8. 현재 미감을 가장 크게 망치는 요소 3개

1. **`fillText` 튜토리얼/타이머** — 에셋은 픽셀인데 HUD만 다른 프로그램.
2. **어두운 상호작용물 (케이블, 앰프, 검정 조끼)** — 팔레트 역할 INTERACTABLE을 안 지킴.
3. **캐리 실루엣** — 16×24 캐릭터 규칙이 아이템을 머리 위에 올리는 순간 깨짐.

---

## 9. 효과가 클 수정 5개

| ID | 수정 | 왜 큰가 |
|---|---|---|
| A | 케이블 하이라이트 + 앰프 잭/그릴 명도 | “무엇을 가져가 / 어디에 꽂나”가 1초에 보임 |
| B | 무대↔백스테이지 1px 경계 + 백 바닥 한 단계 더 어둡게 | 내가 어디 있는지 |
| C | 든 아이템을 바라보는 앞쪽, 캐릭터를 가리지 않게 | 협동 가독성 |
| D | 인게임 텍스트 제거. 튜토리얼은 `!` + 케이블 bounce만 | 미감 + PASS 21 튜토리얼 원칙 |
| E | 관객 파랑 제거, 케이블 `!`는 앰프 / 줄 `!`는 기타 | 나 vs 사고 종류 |

---

## 10. 수정 우선순위

```text
READABILITY
  A 케이블/앰프 대비
  B 구역 경계
  E 관객 색 + ! 좌표 분리
  C 캐리 위치
↓
CONSISTENCY
  D fillText 제거 (픽셀 폰트 숫자 타이머는 그 다음)
  이동 렌더 floor 스냅
  globalAlpha 파동 제거 (마이크 주변 픽셀만)
↓
아직 하지 말 것
  카메라 줌 (PASS 4)
  이동/집기 juice (PASS 5~7)
  새 사고/맵/역할
```

추천 승인 묶음: **A B C D E**  
이건 기능 추가가 아니다. 이미 있는 화면을 읽히게 만드는 것이다.

---

다음: 위 묶음을 승인하면 PASS 2 Color / 해당 스프라이트만 수정하고 멈춘다.
